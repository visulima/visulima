import { describe, expect, it } from "vitest";

// TODO: ADD normal tests
describe("dummy", () => {
    it("should pass", () => {
        expect.assertions(1);

        expect(1).toBe(1);
    });
});
