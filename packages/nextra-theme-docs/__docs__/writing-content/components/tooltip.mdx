import { Tooltip } from "@visulima/nextra-theme-docs/components";

<Tooltip tip="This is a tooltip!">Hover over me</Tooltip> and see a tooltip in action

```mdx
<Tooltip tip="This is a tooltip!">Hover over me</Tooltip>
```

## API

The `Tooltip` component takes the following props:

### `tip`

The text to display in the tooltip

-   Type: `string`
-   Required: `true`

### `children`

The element to wrap with the tooltip

-   Type: `React.ReactNode
-   Required: `true`
