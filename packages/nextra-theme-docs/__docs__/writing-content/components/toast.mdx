import { toast } from "@visulima/nextra-theme-docs";

<button onClick={() => toast("Hello World")}>Toast</button>

```mdx
import { toast } from "@visulima/nextra-theme-docs";

<button onClick={() => toast("Hello World")}>Toast</button>
```