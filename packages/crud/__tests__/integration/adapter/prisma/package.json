{
    "name": "crud-prisma-integration",
    "dependencies": {
        "prisma": "^4.16.2",
        "@prisma/client": "^4.16.2"
    },
    "scripts": {
        "postinstall": "prisma generate && prisma db push && prisma generate"
    }
}
