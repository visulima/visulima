# vis CLI — TODO

- [ ] **SBOM generation command (`vis sbom`)** — Build a lightweight CycloneDX SBOM generator with fewer dependencies than `@cyclonedx/cdxgen`. Should support JS/TS ecosystems, monorepo recursion, and default to `pre-build` lifecycle for security.
