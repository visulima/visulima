# vis — Priority Roadmap

Distilled from `competitive-analysis.md` (Sections 1–8). 10 items ordered by combined leverage × effort × demand-signal.

Demand signal is graded from issue mining: **★★★** = 3+ competitor repos ask for it; **★★** = 2 repos; **★** = 1 strong signal or novel.

---

## Design axes

Cross-cutting principles that constrain every item below, not features in their own right. An item that violates an axis is wrong even if it ships its own scope cleanly.

### Axis A — Agent-first machine-readable output

Every command that produces a result MUST offer deterministic, schema-stable structured output. This is an axis, not a feature, because items 5 (MCP server) and 8 (self-healing CI) are _programmatic consumers_ of the CLI: every command is part of their input surface, so output instability anywhere is a correctness bug for the AI bets — not a cosmetic one. The scattered `--json` / `--format=json` work (items 1, 2, 20, 29; competitive-analysis Themes M + Z) is this axis showing through one command at a time; stated here so future commands inherit the contract instead of re-deriving it.

**Contract every result-producing command upholds:**

- **stdout is the machine channel only.** Structured payload on stdout, _nothing else_; all logs, progress, diagnostics, and prompts go to stderr. The MCP server already enforces this (`@visulima/vis-mcp` keeps JSON-RPC stdout pristine) — the CLI must match so an agent can pipe stdout straight into a parser.
- **deterministic.** Same inputs → byte-identical output. Locale-aware sort on any collection before emission (the pattern item 11 established for cache hashing applies to output too); no map/Set iteration order, no timestamps or absolute paths unless semantically required.
- **CI-gateable exit code.** Process exit reflects success/failure independent of `--format`, so `vis … --format=json` is usable as a pipeline gate (the pattern item 20 `replay` established).
- **stable field names.** JSON keys are a contract, not an implementation detail; additive changes only. Outputs should be backed by a documented type/schema (`schemas/`).

**Canonical flag — `--format=table|json|ndjson`.** New commands MUST use `--format`; it is strictly more expressive than a boolean (`ndjson` serves jq/streaming consumers, `table` stays the human default). The existing boolean `--json` flags (`why`, `info`, `ignore`, `action-graph`, `toolchain`, `generate`, `cache why/hash`) remain as **documented back-compat aliases** — the shipped contract is not broken and no churn is forced. Convergence is tracked, not scheduled, as Tier B item 36.

---

## Top items — ranked

### 1. `vis cache why <task>` + `vis cache hash <task>` ✅ shipped

**Leverage:** Flagship UX. **Effort:** S (days). **Demand:** ★★★

Shipped as `vis cache why <task>` and `vis cache hash <task>` (the bare `why` and `task-why` names were already taken with different semantics). `cache why` reads `.task-runner/last-summary.json`, finds the task, and diffs `hashDetails` (`command`, `nodes`, `runtime`, `implicitDeps`) against the previous run in `.task-runner/runs/` to pinpoint what rotated the hash. `cache hash` prints the stored hash + per-bucket breakdown. Both support `--json` and `--run <id>`.

**Sources:** Theme K + M. nx#18754, turborepo#937, lage#82/#688/#695, moon#2174, wireit#1315.

**Why first:** Days of work, biggest "wow" moment in adoption, and the universal pain point users hit on every other tool. No competitor ships this cleanly.

---

### 2. REAPI gRPC backend + explicit `cacheMode: read | write | readwrite` ✅ shipped

**Leverage:** Strategic moat. **Effort:** L (weeks). **Demand:** ★★★

Shipped across `@visulima/task-runner` and `vis`:

- **Backend abstraction** (`task-runner/src/backends/{types,factory,http,reapi}.ts`): pluggable `RemoteCacheBackend` interface with `retrieveAction` / `storeAction` / `containsAction` / `fetchBlob` / `close`. Factory routes by `backend: "http" | "reapi"`.
- **HTTP backend**: existing Turborepo wire format preserved; `AbortSignal.timeout` for hung-request safety.
- **REAPI backend** over `@grpc/grpc-js`: `Capabilities` negotiation, `GetActionResult` / `BatchReadBlobs` / `Read` / `FindMissingBlobs` / `BatchUpdateBlobs` / `Write` / `UpdateActionResult`. In-flight blob upload dedup. `allowInsecureBearer` guard refuses to send a bearer token over `grpc://` cleartext.
- **v2 CAS layout** (`task-runner/src/cas/{paths,digest,store,action-cache}.ts`): content-addressed `<cacheDir>/v2/{ac,cas,task-hash-index}/<aa>/...`; reader honours both v2 and legacy. Writer dual-emits legacy for portability during the alpha rollout — `vis cache migrate` deferred since alpha caches are throwaway.
- **`cacheMode`**: `mode: "read" | "readwrite" | "write"` on `taskRunnerOptions.remoteCache` replaces the prior `read`/`write` boolean pair. `--cache-mode` / `--cache-backend` flags on `vis run`.
- **`vis cache doctor`**: probes URL reachability for HTTP, calls `Capabilities` for REAPI, reports digests + max batch size + latency. `--format=json` for CI gating.
- **Docs**: `task-runner/docs/concepts/remote-caching.mdx` covers both backends, `bazel-remote` Docker quickstart, action-digest derivation, capability matrix, env var compat (`TURBO_API` / `TURBO_TEAM` / `TURBO_TOKEN`), and the read/write lifecycle.

Per Decision 1, no server binary ships in this repo — `bazel-remote` (Docker, S3, NAS) is documented as the self-host path.

**Sources:** Section 7 Theme A + 8.3 #3. moon (REAPI shipped), Bazel/Buck2/Pants ecosystem, turborepo#683.

**Why critical:** Unlocks bazel-remote, BuildBuddy, BuildBarn, EngFlow as drop-in backends. Without it, vis is yet-another-HTTP-cache.

---

### 3. Watch UX bundle: Vitest keybinds + Windows-clean SIGINT + runtime-doctor ✅ shipped

**Leverage:** First-impression. **Effort:** M. **Demand:** ★★★

Shipped across five commits: Vitest-style `r/Enter/a/p/q/Ctrl+C/h/?` keybinds for `vis run --watch` (`watch-keybinds.ts`); SIGTERM→SIGKILL escalation watchdog with per-target `killGracePeriodMs` so the timeout becomes a hard upper bound (`signal-escalation.ts`); Windows graceful path via `CREATE_NEW_PROCESS_GROUP` + `GenerateConsoleCtrlEvent(CTRL_BREAK_EVENT)` in the task-runner native bindings, falling back to the JobObject-on-drop hard kill; runtime diagnostics in `vis doctor` covering inotify capacity (Linux), TTY availability, and orphaned `vis`/`task-runner` processes; auto-recover via `vis doctor --fix`, which reuses the same matcher as the diagnostic and SIGTERMs every flagged PID (escalates to SIGKILL on `force`, treats `ESRCH` as success).

The "daemon-doctor" framing in the original spec was an over-claim — task-runner has no long-running daemon today, so the closest analogue is the orphan-watcher leak, and the runtime checks plus `--fix` close that gap. A genuine cache/file-watcher daemon would be a separate Phase 3 item under #2 (REAPI server binary).

**Sources:** Section 7 Theme B + 8.2. turborepo#12651/#12652/#4608, nx#28513/#35036, wireit#788/#790.

---

### 4. Cache retention & GC (`vis cache prune`) ✅ shipped

**Leverage:** Production pain killer. **Effort:** S–M. **Demand:** ★★★

Shipped: `vis cache prune` already supported `--max-age-days` and `--max-size`; this round added `--keep-last <N>` (sort entries newest-first by mtime, drop everything past N before age/size eviction runs). Universal foot-gun across nx#35329, lage#921, lage#120, wireit#71 — no JS-land competitor ships clean retention. Direct production-pain win.

**Sources:** Section 8.1 Theme N.

---

### 5. MCP server + generator/template introspection ✅ shipped

**Leverage:** AI-tooling year. **Effort:** M. **Demand:** ★★★

Shipped as `@visulima/vis-mcp`, an MCP stdio server with eight read-only tools and a paired Claude Skill that documents optimal usage:

- **Workspace introspection**: `list_projects` (with vis-query filter), `describe_project`, `list_targets` (per-target rows with type, command, description).
- **Template introspection**: `list_templates`, `describe_template` (variable schema for `vis generate`) — closes the moon#2437 "agents fly blind" gap.
- **Run forensics**: `get_run_logs` (reads `.task-runner/last-summary.json` or a specific `runId`, optionally filters to one task), `cache_why` (hash-rotation diff), `cache_hash` (recorded hash + per-input details).
- **Resolution path**: `createRequire` from `VIS_MCP_WORKSPACE_ROOT` finds the workspace-local `@visulima/vis`; `VIS_MCP_VIS_BIN` overrides for linked checkouts.
- **Wire safety**: argv-form spawn (no shell), `isValidTaskId` / `isValidRunId` guards reject leading-`-` flag injection and path traversal, all logs go to stderr to keep the JSON-RPC stdout pristine.

**Design decision — read-only by intent.** The roadmap originally named `run_task`. We deliberately diverged to the Nx-style "agent prepares, human executes" model: the MCP surface stays read-only so an LLM client cannot mutate the workspace through the protocol. Mutating commands (`vis run`, `vis generate`) are something the agent recommends and the human invokes. README.md and `__tests__/server.test.ts` codify the eight-tool contract.

**Sources:** Section 7 Theme F + 8.1 Theme Z. Nx MCP, moon#2437.

---

### 5b. Worktree-aware shared cache ✅ shipped

**Leverage:** AI-tooling parity. **Effort:** S–M. **Demand:** ★★★

Shipped: default cache resolves to `<mainWorktreeRoot>/.task-runner-cache` whenever the workspace is a linked git worktree, so N parallel agents in N sibling checkouts share one store. Detection lives in the task-runner napi binding (`packages/tooling/task-runner/native/src/worktree.rs`): `.git` as a regular file (gitlink) → shell out to `git rev-parse --git-common-dir` and resolve to its parent. Memoized per canonicalized workspace root for the process lifetime; symlinked `.git` files agree with primary-checkout detection (both follow symlinks via `fs::metadata`). Single-checkout repos and primary worktrees behave unchanged.

The `vis cache list/prune/size` commands accept `--scope=shared|worktree|all`. `shared` is the default and reads `<mainWorktreeRoot>/.task-runner-cache`; `worktree` operates on the linked checkout's local cache; `all` runs against both with dedup when they resolve to the same path. `vis cache clean` deliberately omits `--scope` — the existing out-of-workspace prompt already requires `--force` from a linked checkout.

Hermetic-experiment users opt out via `sharedWorktreeCache: false` in `vis-config.ts`. Explicit overrides (`--cache-dir`, `taskRunnerOptions.cacheDirectory`, `VIS_CACHE_DIRECTORY` env — added for Nx parity) win over both worktree-share and the default. Tests cover the resolver matrix, cross-worktree read/write, 6-way concurrency stress, --scope CLI dispatch, and the symlinked-`.git` regression.

**Sources:** Nx 22.7 (PR #34942, `packages/nx/src/native/worktree.rs`, `packages/nx/src/utils/cache-directory.ts`).

---

### 6. `$AFFECTED_FILES` token + conditional tasks + finally tasks ✅ shipped

**Leverage:** Expressiveness. **Effort:** M. **Demand:** ★★

Shipped across three pieces:

- **Affected-files forwarding** via per-target `options.affectedFiles: "args" | "env" | "both"` (`src/task/types.ts:35`, `src/commands/run/handler.ts:53,93,372`). Sets `VIS_AFFECTED_FILES` env (newline-separated) and/or appends paths as args.
- **`${affected.files}` / `${changed_files | flag '--file'}` tokens** wired through `expandTokensInString` (`@visulima/task-runner/src/command-parser/expand-tokens.ts`) at `src/commands/run/handler.ts:109,419`. Tokens expand at the user-specified position with project-root scoping (out-of-project paths dropped, in-project paths rewritten relative to project root). Supports the bare token (`eslint ${affected.files}`) and the flag form (`stylelint ${changed_files | flag '--file'}`); a leading backslash escapes (`\${affected.files}`). Coexists with `affectedFiles: "args"` — token wins position, mode appends at the end. End-to-end coverage in `__tests__/commands/run/run-affected-tokens.test.ts`.
- **Conditional tasks** via `WhenCondition` in `@visulima/task-runner` (`packages/tooling/task-runner/src/when-condition.ts:18`, `evaluateWhen`, `explainWhen`). Top-level `when:` on targets (`types.ts:90,231`) covers os/env/branch/ci.
- **Finally / always-run tasks** via top-level `always: true`. Pulled out of the main graph in `default-task-runner.ts:187` and run sequentially after the main task graph in `task-orchestrator.ts:24,231`.

**Sources:** Section 7 Theme C/D + 8.1 Theme Q. moon#1865/#1666/#1815/#2260, wireit#168/#325, nx#14010.

---

### 7. Output styles: quiet-on-success / verbose-on-failure ✅ shipped

**Leverage:** Universal QoL. **Effort:** S. **Demand:** ★★★

Shipped: `vis run --output-style=quiet` swallows stdout/stderr from successful and cached tasks while keeping failures fully visible (skipped tasks still print their reason). Per-target override via `options.outputStyle: 'normal' | 'quiet'` in `vis-task.ts`/`vis-config.ts` — a single noisy linter can opt into quiet mode without changing the global flag, and a critical task can stay verbose under a global `quiet` setting. Unknown `--output-style` values fall back to `normal` so a typo never silently mutes output.

**Sources:** Section 8.1 Theme T. moon#2475/#2451/#1930/#2424/#2146, lage#634, wireit#1303/#56.

---

### 8. Self-healing CI on top of `vis ai` ✅ shipped

**Leverage:** Strategic differentiator. **Effort:** M–L. **Demand:** ★ (novel)

Shipped as `vis ai heal` (proposal loop) + `vis ai heal accept` (auto-commit). The v1 loop reads the latest failed task, asks the configured AI provider for a structured patch, applies it, validates it by re-running the task on the CI runner, and posts a markdown summary to the PR (GitHub Actions) or MR (GitLab CI). When a maintainer comments `/vis heal accept`, a follow-up workflow re-derives the proposal, validates it, and commits via the platform's REST API. Implementation:

- **CI provider detection** (`src/ai/ci-context.ts`): dispatches on `GITHUB_ACTIONS=true` and `GITLAB_CI=true`; falls back to `GITHUB_EVENT_PATH` JSON for the PR head SHA when `GITHUB_REF` isn't a `refs/pull/<n>/...` ref. GitLab side reads `CI_MERGE_REQUEST_IID`, `CI_API_V4_URL`, and `GITLAB_TOKEN` (auto-injected `CI_JOB_TOKEN` is intentionally rejected — it cannot post MR notes).
- **Comment posting** (`src/ai/pr-comment.ts`): GitHub side prefers bundled `gh pr comment` and falls back to the REST `/issues/{n}/comments` endpoint when gh is missing or fails; GitLab side goes straight to REST `/projects/{enc}/merge_requests/{iid}/notes` with the `PRIVATE-TOKEN` header. Argv-form spawn — no shell metachars in the path. Comment body capped at 60 KB so a multi-file patch can't trip GitHub's 65 KB ceiling; markdown fence auto-escalates so patches containing triple backticks render correctly.
- **Heal orchestrator** (`src/commands/ai/heal.ts`): split into reusable phases (`findHealCandidate` → `proposeAndApply` → `validateAppliedFix` → `postHealComment`) so `accept` can replay the propose path deterministically. Public flags unchanged: `--dry-run`, `--validation-timeout`, `--no-cache`.
- **Auto-commit** (`src/commands/ai/heal-accept.ts` + `src/ai/git-commit.ts`): triggered by `/vis heal accept` in a PR/MR comment. Validates the actor against `ai.heal.allowedActors` (vis-config), refuses fork PRs (token has no write access), resolves the head ref, replays the propose flow, then commits via Octokit's Git Trees API (GitHub) or Gitbeaker's `Commits.create` (GitLab). GitHub commits are auto-signed as `github-actions[bot]` because they're created with `GITHUB_TOKEN`; GitLab v1 ships unsigned. Both SDKs are **optional peer-deps** — the lazy loader (`src/ai/sdk-loader.ts`) prompts the user to install on first run in an interactive shell, or fails loud with the install command in CI. After a successful commit, a confirmation comment with the new SHA is posted to the PR/MR.
- **Tests**: `ai-ci-context.test.ts`, `ai-pr-comment.test.ts`, `ai-heal.test.ts` cover env detection, comment dispatch, and the rendering layer; `ai-sdk-loader.test.ts` covers prompt/install/decline/import-error paths; `ai-git-commit.test.ts` covers the GitHub Git Trees ordering, GitLab `Commits.create`, and missing-token / missing-API-base errors; `ai-heal-accept.test.ts` covers trigger parsing (GitHub event payload + GitLab env-var bridge), actor allow-list, fork rejection, and missing-trigger-phrase no-op.

**Sources:** Section 4 Tier-1 #4. Nx is the only competitor with this; massive moat in JS-land.

---

### 9. Package-level + extends config layering ✅ shipped

**Leverage:** Architecture-shaping. **Effort:** M. **Demand:** ★★

Shipped: root `vis-config.ts` + per-package `vis.task.ts` overlay with explicit merge semantics, plus an `extends` chain on the root config.

- **Extends resolver** (`src/config/config.ts:205-252,343,373,391`): relative paths resolved against the parent file; bare specifiers (`@acme/preset`, `vis-preset-foo`) resolved via npm from the parent file; absolute paths rejected. Recursive load with cycle protection, post-order fold; `extends` is consumed during resolution and never propagated downstream.
- **Per-package overlay** (`src/config/config.ts:22,105,494-542`): `vis.task.ts` discovered per project, compiled via jiti, cached by content hash so editing one project's overlay doesn't invalidate others. `defineVisTaskConfig` helper provides type safety.
- **Merge semantics** (`src/config/workspace.ts:434-514`): per-project targets from `project.json` merge with the overlay via `mergeProjectTargets`; overlay wins per-target. `loadVisTaskConfigs` (`workspace.ts:609-649`) pre-loads overlays in parallel; legacy callers keep the no-overlay path.

**Sources:** Section 8.1 Theme S.

---

### 10. Shared services / sidecar registry across invocations ✅ shipped

**Leverage:** Cross-invocation devloop. **Effort:** M. **Demand:** ★★★

Shipped: long-lived DB/mock/devserver lifecycle that survives across multiple `vis run` calls within a shell session. Per-workspace registry under `~/.vis-services/<sha256(workspaceRoot)[:12]>/` with atomic JSON entries and an exclusive-create file lock for concurrency.

- **`vis service start|stop|list|status|restart|logs <id>`** (`src/commands/service/`): detached spawn (`spawn`, `unref`, log fd → 0o600 file), POSIX liveness via `process.kill(pid, 0)`, SIGTERM-then-SIGKILL with configurable grace, table/json list output, `logs --follow` with abort-aware tail loop and SIGINT exit code 130. `service stop --all` and `service restart` round-trip the same lifecycle.
- **TCP readiness probe** (`src/services/readiness.ts`): `runReadiness({ tcp: { port, host, timeoutMs } })` polls until success or timeout. Used both at start (refuse to register a non-listening service) and at attach (demote a registered-but-dead listener so the user gets a "service not running" diagnostic instead of a silent half-run).
- **`vis run` auto-attach** (`src/commands/run/apply-service-registry.ts`): for every task in the graph whose target carries `service: ServiceConfig`, look up the alive registry entry, probe in parallel, prune satisfied services from the graph and merge their `env` into transitive dependents (BFS over the original deps, deterministic alphabetical merge for last-write-wins). Orphan services with no dependent and unregistered services that _only_ exist as a dep emit an actionable diagnostic instead of cold-starting.
- **`VisTargetOptions.service`** (`src/task/types.ts`): the presence of this block — not `preset: "server"` alone — is what makes a target eligible for the registry, so existing in-run-only persistent tasks are unaffected.
- **Tests**: 20 registry, 11 lifecycle, 11 service-handler, 14 apply-service-registry, plus a 2-case integration test (`__tests__/services/lifecycle.integration.test.ts`) that drives `start → list → attach (with the same probe wired in run/handler) → stop → re-attach diagnostic` against a real TCP listener, and the wrapper-alive/server-dead demotion path.

**Sources:** Section 8.1 Theme R. wireit#580 (8 thumbs), moon#1365, moon#2003, rushstack#1151.

**Why critical:** No JS-land competitor ships shared services cleanly. Direct fix for the "I keep restarting Postgres between every test run" papercut, and a flank Turbo/Nx can't easily close without rearchitecting their daemons.

---

### 11. Cache restoration fidelity (timestamps, perms, ordering) ✅ shipped

**Leverage:** Correctness foundation. **Effort:** S–M. **Demand:** ★★

Shipped across `@visulima/task-runner` and `vis`:

- **Archive round-trip preserves mtime + mode bits** (`task-runner/src/archive.ts`): tar streams capture and restore file timestamps and permission bits, so cache hits no longer reset mtime to "now" or drop the executable bit.
- **Deterministic readdir ordering** (`task-runner/src/cache.ts`): directory entries are sorted with a locale-aware comparator before hashing and archive emission, so two runs that produce the same files always produce the same digest regardless of FS order.
- **Per-target `cacheRestore` config** (`task-runner/src/types.ts`): `cacheRestore: { preserveMtime, preservePerms }` defaults on; opt-out per target for hermetic-experiment cases.
- **`vis cache verify <task>`** (`vis/src/commands/cache/handler.ts`, `index.ts`): diffs the cached archive against the live workspace, surfacing content/mtime/perm drift. `--scope=all` searches across worktree + shared caches; per-file diff runs in parallel with bounded concurrency. Missing entries exit 1 (documented).

**Sources:** Section 8.1 Theme O. wireit#1316, lage#766, lage#839, lage#690.

---

### 12. Attested provenance & keyless-signed cache (Sigstore) ✅

**Leverage:** Strategic moat (unclaimed in JS-land). **Effort:** M. **Demand:** ★★★ (was ★ "novel/absence-is-the-signal" — upgraded 2026-05-18: the Aug 2025 s1ngularity attack on Nx is the concrete, citable incident that hardens this from inference to fact).

**Lead — the category's defining trust event.** On 26 Aug 2025 attackers exfiltrated Nx's npm publish token via a `pull_request_target` injection and shipped malicious `nx` / `@nx/*` / `@nrwl/*` versions whose **postinstall** script harvested credentials (npm/GitHub tokens, SSH keys, `.env`, wallets), **weaponised locally installed AI CLIs (Claude/Gemini/`q`) for recon** (a first), and base64-exfiltrated the loot into a public `s1ngularity-repository` on each victim's own GitHub. Second wave: 190+ orgs, 3000+ repos, 2,349 secrets. Nx's fix — npm Trusted Publishers (OIDC) + manual release approval — is the exact provenance/keyless direction this item ships, retrofitted under fire. The failure modes map 1:1 onto vis's posture: **no postinstall at all** (the entire payload was a postinstall hook; statically verifiable, not a promise); the inbound `provenance` marshall flags exactly the "prior version had `dist.attestations.provenance`, this one dropped it" compromised-CI-key fingerprint; the inbound `signatures` marshall catches the forged/unsigned publish; `vis attest` gives outbound SLSA provenance the rogue packages never could; and the MCP surface (item 5) is read-only by intent, so the AI-CLI recon rung that made s1ngularity novel is structurally inert in vis. This is the cleanest "we do supply chain, they don't" wedge in the product — now backed by an incident, not an absence.

The outbound half of supply-chain security. All three parts shipped:

- **(a) Inbound — ✅ shipped.** `src/security/marshalls/provenance.ts` (flags a dependency that dropped `dist.attestations.provenance`) and `signatures.ts` (ECDSA-verifies registry signatures against npm's keys). This is the part competitors don't have.
- **(b) Outbound cache authenticity — ✅ shipped.** Keyless sign + verify of remote-cache tarballs, layered **above** item 28's HMAC (HMAC = integrity, forgeable by any cache reader; keyless = authenticity + non-repudiation + Rekor transparency). task-runner stays **dependency-free**: `backends/types.ts` exposes `RemoteCacheAttestation` (`signArtifact` / `verifyArtifact` / `requireOnDownload` callbacks, mirroring the `onUploadError` hook style) and the HTTP backend (`backends/http.ts`) moves the opaque bundle through the `X-Artifact-Attestation` header — sign after the HMAC on PUT, verify after HMAC on GET, missing-header + `requireOnDownload` ⇒ cache miss, bad bundle ⇒ reject. vis owns the Sigstore dependency: `src/security/sigstore/cache-attestation.ts` builds the hooks (`buildCacheAttestationHooks`), wired in `commands/run/handler.ts` only when `remoteCache.attestation` is configured. REAPI deliberately skips it — gRPC integrity rides CAS sha256. Keyless needs ambient CI OIDC; outside CI `signArtifact` returns `null` (skip-with-warn).
- **(c) `vis attest` + `vis attest verify` — ✅ shipped.** `commands/attest/` — `vis attest <artifact>` emits a SLSA v1 / in-toto Statement (`predicateType: https://slsa.dev/provenance/v1`), keyless-signs it via Sigstore, writes a `.sigstore` bundle (`--output`, `--require-signing` to make missing OIDC fatal, else skip-with-warn). `vis attest verify` composes the inbound marshalls (provenance regression ⇒ `warning`, signature findings pass through severity) into one CI-gateable command — `--format=json|ndjson` to stdout (Axis A), `--fail-on=error|warning`, `--prod-only`, `--allowlist`; sets `process.exitCode = 1` on gating findings. Sigstore is an **optional peer dep**: `src/security/sigstore/loader.ts` lazy-imports it (mirrors the AI SDK loader) — interactive prompt-to-install, CI fail-loud with `pnpm add -D sigstore`. Tests: sigstore-loader (6), cache-attestation (6), attest handler (4), task-runner http attestation (5).

**Sources:** competitive-analysis §8.6 (s1ngularity lead) + §4 Tier-1 #9 + §3 baseline. s1ngularity — nx.dev/blog/s1ngularity-postmortem, thehackernews.com/2025/08/malicious-nx-packages-in-s1ngularity.html, infoq.com/news/2025/10/npm-s1ngularity-shai-hulud. Provenance — docs.npmjs.com/generating-provenance-statements, slsa.dev/spec/v1.0, github.com/actions/attest-build-provenance, docs.sigstore.dev/cosign/signing/signing_with_blobs.

**Why it matters:** Inbound already shipped unclaimed; (b)+(c) convert a quiet moat into a stated one. No JS-land runner reaches past HMAC integrity — the cleanest "we do supply chain, they don't" wedge, and it stacks on #28 rather than competing with it.

---

### 13. Migration equivalence verification (`vis migrate verify-graph` / `vis export --verify`) — inbound ✅ shipped

**Leverage:** Adoption-trust foundation (makes "adopt/leave risk-free" _provable_, not a promise). **Effort:** M. **Demand:** ★ (novel — absence is the signal: Nx Console ships a migration _diff preview_, nobody proves graph equivalence).

Shipped as `vis migrate verify-graph` (the bare `verify` name was already taken by the gitleaks/secretlint/syncpack/sherif leftover-scanner — same rename precedent as item 1's `cache why`). `src/commands/migrate/equivalence.ts`: independent source parsers for turbo.json (tasks/pipeline + globalDeps/globalEnv), nx.json `targetDefaults` + per-project `project.json`, and `.moon/tasks.yml` + per-project `moon.yml`; a `loadVisConfig`-backed vis-side model; one normalized `TaskGraphModel` (canonical `^t` / `p#t` / `t` edge tokens unifying all four dependsOn syntaxes, object inputs JSON-canonicalized, locale-sorted everywhere per Axis A); `diffModels` over the target-set / dependsOn / inputs / outputs / env / cache axes; turbo's by-design `project#task` skip reported as a non-fatal `warning` (honest, not a false `error`). `--format=table|json|ndjson` (JSON/NDJSON to stdout only), `--from` auto-detect, `--fail-on=error|warning`, `process.exitCode=1` on gating divergence. The outbound `vis export --verify` half remains open — gated on the unshipped outbound migrator (Tier B / 7.3 #9).

The migration story previously rested on **coverage, not correctness**. §3 ships inbound migrators (turbo/nx/moon → `vis-config.ts`); 7.3 #9 plans outbound (`vis export → turbo/nx/moon.yml`); §7.4 #5 sells "low-risk to adopt, low-risk to leave." Every one of those claims is a leap of faith until the migrated config is _proven_ to yield the same task graph. This is the migration analogue of item 11: coverage without fidelity is unproven. Cheap to ship because the pieces already exist — the inbound migrators already parse every source format, and vis already builds + serializes its own task graph **deterministically** (Axis A).

- **`vis migrate verify-graph` (inbound) ✅.** Builds the _source's_ intended graph model from `turbo.json` / `nx.json` / `moon.yml`, builds vis's resolved graph from the migrated config via `loadVisConfig`, and asserts structural equivalence on the shipped axes: (1) same target set per project; (2) same dependency edges (canonical-token isomorphism on the project×target node set, not just edge count); (3) same **cache-key surface** — inputs/outputs/env normalized + locale-sorted (item 11's comparator) so the migrated config can't silently widen/narrow what invalidates cache; (4) cache-flag parity. _Deferred sub-scope:_ same **affected closure** against a synthetic changeset / `--since <ref>`, and byte-equal resolved-command compare after token expansion — both are additive follow-ups on the shipped model, not blockers.
- **`vis export --verify` (outbound, pairs with 7.3 #9).** After emitting the foreign config, re-import it through vis's _own_ inbound migrator and assert a round-trip fixpoint (`import ∘ export ≈ identity` on the graph model). This makes "you can leave any time" provable **without** depending on turbo/nx/moon being installed. Optional `--cross-check` shells out to the real tool's dry-run/graph command (`turbo run --dry=json`, `nx graph --file`, `moon query tasks`) when present and diffs the foreign task graph for a second, independent witness.
- **Output contract (Axis A).** `--format=table|json|ndjson`; non-equivalence ⇒ `process.exitCode = 1` so it's a CI gate; `--fail-on=error|warning`. The diff report names exactly which node/edge/input diverged — same UX contract as `vis cache why` and `vis attest verify`, so the failure is actionable, not "graphs differ."
- **Scoping honesty (mirrors item 3's daemon-doctor / item 12's HMAC-vs-keyless framing).** Equivalence is defined on the **graph model + cache-key surface + affected closure**, _not_ on executing both runners and diffing build artifacts — that path is non-hermetic and tool-install-dependent. The provable, deterministic core is the claim; document the boundary hard so "verified" is never read as "bit-identical outputs across runners."

**Sources:** competitive-analysis §7.3 #9 + Theme J (the inbound-correct / outbound-missing insight) + §3 inbound baseline; Nx Console Migrate UI (diff preview only — no equivalence proof).

**Why it matters:** Converts the entire migration narrative from a leap of faith into a CI-gateable proof. "Adopt risk-free" / "leave any time" is only credible if the migration is shown to preserve semantics — this is the command that shows it, and no JS-land competitor has it.

---

### 14. Source-mapped, code-framed task-failure rendering — the `@visulima/error` cross-package moat

**Leverage:** Structural moat (copy-proof, in-monorepo, unclaimed). **Effort:** M. **Demand:** ★ (novel / absence-is-the-signal — same class as #12 pre-upgrade: no competitor files issues for a capability none of them can ship).

**The differentiator the analysis enumerated 30+ borrowable features around but never named.** Every other item on this roadmap is a feature vis can build or borrow. This one vis _already owns_ and isn't using: `@visulima/error` (`packages/error-debugging/error/`, v6.0.0-alpha.25) ships `parseStacktrace` (V8/Firefox/Safari/Node/React-Native dialects), `codeFrame` (syntax-highlighted, configurable context), `renderError`, `getErrorCauses`, plus source-map resolution via `@visulima/source-map` (3.0.0-alpha.9) and a CLI renderer already packaged as `@visulima/error-handler/handler/cli`. vis lives in a monorepo whose flagship brand _is_ error-debugging (`@visulima/error`, `pretty-error`, `code-frame`). **No competitor — Turbo, Nx, moon, Lage, Rush, Bazel-family — can render a failed task's stack trace source-mapped, syntax-highlighted, and code-framed, because none of them owns an error-rendering library.** They pass stderr through raw. So does vis today: failure output is pure PTY passthrough (`src/tui/components/task-store.ts:168-169`, 256 KB buffer at `:190`; rendered unprocessed in `output-panel.tsx:99-145`; summarised as bare task-IDs in `command-summary.tsx:114-131`; raw fetch in `vis-task-runner-app.tsx:215`). Neither `@visulima/vis` nor `@visulima/task-runner` depends on `@visulima/error` — zero imports. The moat is free, native, sitting unused.

**Why it's structurally copy-proof.** A competitor closing this gap must build or acquire a stack-trace parser + source-map resolver + syntax-highlighting code-framer and keep it current across V8/JSC/SpiderMonkey trace dialects — a multi-month project orthogonal to a task runner's roadmap, and _exactly_ the Visulima org's existing flagship product. Same shape as #12 (we do supply-chain, they don't): a wedge that exists because of what the surrounding monorepo _is_, not because of effort spent inside vis. Unlike #12 it needs no external dependency, no CI OIDC, no transparency log — the lowest-cost structural moat in the analysis.

**Compounding with the AI moat (#8).** The same parsed-trace + code-frame structure that helps a human is a far better LLM input than raw stderr. `vis ai heal` currently feeds the model raw failure text; a structured frame (resolved source location + offending lines + cause chain) measurably improves heal-proposal quality — and competitors can't match the _input quality_ feeding their (hypothetical) heal loop without the error library either. The moat stacks onto an already-shipped flagship rather than competing with it.

**Shipping shape (proposed, not yet built):**

- **Render path.** On non-zero exit, if captured output contains a parseable trace, run `parseStacktrace` → resolve frames through `@visulima/source-map` → emit a `codeFrame` + `renderError` block in the TUI failure panel and the final summary. No parseable trace (rust/go/python tasks, opaque tool output) ⇒ fall back to today's raw passthrough verbatim — never lose information.
- **Dependency placement mirrors #12 / §8.6.** vis owns the `@visulima/error` dep; `@visulima/task-runner` stays dependency-free (it already carries `terminalOutput`) — rendering is a vis-side concern, optionally exposed as a render hook so task-runner never imports the error stack.
- **Axis A compliance.** The code-framed render is the human (`--format=table`) channel on stderr only. `--format=json|ndjson` stays machine-clean; the _structured_ parsed trace (file/line/col/frame/cause) is an additive, documented JSON field — strictly more for agents, nothing on stdout. Respects `--output-style=quiet` (#7) and `NO_COLOR`.
- **Replay parity (#20).** `vis replay` replays a recorded failure through the same renderer so a post-hoc inspection looks identical to the live run.

**Sources:** Internal — `packages/error-debugging/error/` (`@visulima/error` v6.0.0-alpha.25), `@visulima/source-map` v3.0.0-alpha.9, `@visulima/error-handler` v2.0.0-alpha.24 (`/handler/cli`). competitive-analysis §8.7 (the cross-package moat), §3 baseline, §8.6 (the analogous structural-moat pattern this follows), §4 Tier-1 row #10. Competitor stderr-passthrough confirmed by absence: no error-rendering library in Turbo/Nx/moon/Lage/Rush/Bazel-family product surfaces.

**Why it's a ranked item not Tier B.** The user-stated test: "a bigger moat than half of Tier B." Tier B items are borrowable parity features with real but bounded demand; this is a copy-proof structural differentiator in the same class as #2 (REAPI), #8 (AI heal), #12 (supply chain) — the "what the surrounding monorepo _is_" wedges. It outranks Tier B by leverage even though its raw demand signal is ★ (novel), exactly as #12 did before its 2026-05-18 incident upgrade.

---

## Tier B — file but defer

These have real demand but lower leverage given current vis scope:

| #   | Item                                       | Source                                                      | Why defer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --- | ------------------------------------------ | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 11  | Inferred tasks / Project Crystal ✅        | Theme 4 Tier-1 #5                                           | Ships behind `inferTargets: true`; explicit scripts/project.json/vis.task.ts win per-key. 36 detectors covering app frameworks (nuxt, next, remix, astro, gatsby, docusaurus), bundlers (vite, rolldown, tsdown, tsup, packem, rollup, webpack), server frameworks (nest), test runners (vitest, jest, bun, deno, playwright, cypress), stories (storybook), typecheck (tsc), docs (typedoc, vitepress), lint/format (eslint, prettier, biome, oxlint, oxfmt, stylelint, knip), DB tooling (prisma, drizzle), and release plumbing (graphql-codegen, api-extractor, changeset). Conflict order baked into `BUILT_IN_DETECTORS`: `build` → nuxt > next > remix > astro > gatsby > docusaurus > vite > nest > rolldown > tsdown > tsup > packem > rollup > webpack; `test` → vitest > jest > bun > deno; `test:e2e` → playwright > cypress; `lint` → eslint > biome > oxlint > deno; `format` → prettier > biome > oxfmt; `db:*` → prisma > drizzle. Stylelint uses `lint:css` and knip uses `knip` so they coexist with the primary linter; mutating commands (prettier --write, oxfmt, deno fmt) deliberately omit `type` to skip caching. Dep-only triggers cover convention setups (vitest, jest, eslint, prettier, biome, etc.); transitively-installed tools (vite, rolldown, rollup, webpack, storybook, nest, remix, vitepress, bun, deno, changeset) require a config file. 61 unit tests across `__tests__/inference`.                                                                                                                                                                                      |
| 12  | Test atomization                           | Theme 4 Tier-1 #6                                           | Pairs with distributed agents — pointless without #14                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 13  | Public plugin API ✅                       | todo.md, Theme E                                            | Lives in `src/util/hooks.ts` + `src/config/define-plugin.ts`, built on `hookable` (Nuxt's lib). 14 typed hooks: lifecycle (`run:before/after`, `task:before/after/cacheHit/cacheMiss/failure`), streaming (`task:stdout/stderr`), retry (`task:retry`), fingerprint (`task:fingerprint`), services (`service:start/stop/attach`). Plugins register declaratively via `hooks: {…}` (single fn or array per hook) or imperatively via `setup(hooks)` (unlocks `hookOnce` / `removeHook` / `beforeEach`). `HookableLifeCycle` bridges task-runner's `LifeCycleInterface` so plugin authors never see the lower-level surface. Per-run hook registry — concurrent `vis` invocations in the same process don't share state. Buggy lifecycle plugins warn via `onHookError`; safety-critical hooks (`run:before`, `task:retry`, `task:fingerprint`) propagate throws by contract so a misbehaving plugin can't silently corrupt cache state. Public surface re-exported from `@visulima/vis/config`: `definePlugin`, `VisPlugin`, `VisHooks`, `FingerprintContributor`, plus reference `otelPlugin` + `OtelPluginOptions`. Docs at `docs/guides/plugins.mdx` (quick-start, hook reference, factory pattern, options-vs-plugins decision guide). 75+ tests (32 hooks + 45 otel + cross-package onRetry / onFingerprint coverage in `task-runner/__tests__`).                                                                                                                                                                                                                                                               |
| 14  | Distributed agents (Cobuilds-style)        | Theme 4 Tier-1 #2                                           | Lighter pattern via shared cache + Redis lock                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 15  | Webhooks / pipeline events                 | todo.md, Theme H                                            | ~100 LOC over `LifeCycleInterface`; cheap when needed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 16  | Strict env mode ✅                         | Section 7.3 #14                                             | `strictEnv` config flag + `--strict-env` / `--no-strict-env` CLI override + per-target `options.strictEnv`. Pre-spawn scan extracts `${VAR}` / `$VAR` references from each task's command (skipping POSIX specials and inline-default `${VAR:-fallback}` forms) and fails the task with a single-line error naming every missing variable. Layered precedence: per-target opt-in/out > CLI flag > workspace config > off. 17 unit tests in `__tests__/task/strict-env.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 17  | Jujutsu (jj) VCS support                   | Section 7.3 #1                                              | Abstract VCS layer first; trivial backend afterward                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 18  | Boundaries as `eslint-plugin-vis`          | Section 4 Tier-2                                            | Surface existing layer constraints to ESLint                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 19  | GitLab CI / Buildkite presets ✅           | Section 7.2 G                                               | `vis ci` ref auto-detection now covers `BUILDKITE_PULL_REQUEST_BASE_BRANCH` / `BUILDKITE_COMMIT` alongside the existing GitHub Actions and GitLab CI envs. `CiContext` gained a `buildkite` provider with `buildId` / `buildNumber` for annotation deduplication. `vis ai heal` posts to Buildkite via the `buildkite-agent annotate` CLI (with REST fallback to `/v2/.../builds/{n}/annotations` using `BUILDKITE_API_TOKEN`); annotations key on `vis-ai-heal-${BUILDKITE_BUILD_ID}` so reruns update in place. `vis ai heal accept` uses the Buildkite block-step unblock as the acceptance signal — `BUILDKITE_UNBLOCKER_EMAIL` / `BUILDKITE_UNBLOCKER` is the actor for the allow-list, and the commit derives a GitHub/GitLab `CiContext` from `BUILDKITE_REPO` + matching token. Self-hosted Enterprise honoured via `BUILDKITE_API_BASE_URL`. Reference `.buildkite/pipeline.yml` (with optional heal block step), GitLab MR pipeline, and the env/token matrix are in `docs/guides/ci-cd.mdx`. Bitbucket Pipelines explicitly skipped — out of scope per Tier B re-scoping. Generator parity: `vis generate buildkite-ci` ships as a builtin moon-format template (`templates/buildkite-ci/`) bundled with the package — `discover.ts` resolves it via `import.meta.url` so it works from src/dev and dist/prod alike, and any user-vendored copy at `.vis/templates/buildkite-ci/` overrides the bundled preset. Tests: 14 ci-context (5 buildkite), 13 pr-comment (5 buildkite), 23 heal-accept (9 buildkite), 8 ci-command (1 buildkite), 4 builtin-buildkite-ci produce smoke, 8 discover (3 builtin). |
| 20  | Run replay (`vis replay`) ✅               | Section 4 Tier-2                                            | Reads `.task-runner/last-summary.json` and `.task-runner/runs/<id>.json` (no re-execution). Surfaces: run header (totals, env, duration), per-task table (status, duration, hash, exit code), `--task` detail mode with a `vis cache why` hint, `--failed` filter, `--list` of recorded runs newest-first, `--format=json` stable contract. Exit code mirrors `stats.failed > 0` so it's CI-usable. 12 handler tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 21  | Lockfile / install-awareness preflight ✅  | Theme W, moon#2055, rushstack#5624                          | Lives at `src/preflight/lockfile.ts`. Compares lockfile mtime to PM-specific install marker (`node_modules/.modules.yaml` etc.); warns in TTY, hard-fails in CI. Default-on, opt-out via `--no-preflight` or `preflight.lockfile: false`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 22  | Skip-on-warning incrementality ✅          | Theme P, rushstack#1402                                     | Per-target `warningPattern` + `cacheOnWarning`; result carries `hadWarnings`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 23  | Task descriptions in `--help` / TUI ✅     | Theme Y, wireit#1015, moon#1914                             | Surfaced via `vis list --targets` (existing per-target `description` field)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 24  | Skill suggestions after `vis install`      | midudev/autoskills                                          | Post-install tip mode: reuse existing PM detection to match the project's stack against an autoskills-style map (~80 techs + combos) and print copy-pastable `claude skills install …` hints behind `--suggest-skills`. Tip-only by default — vis stays out of the trust chain; vendoring + auto-install is a separate (heavier) decision that inherits audit + sync responsibility for third-party skill slugs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 25  | URI-based input format ✅                  | moon#1985                                                   | Forward-compat `inputs` string forms in `task-runner/src/parse-input-uri.ts`: `file://path` and `glob://pattern` map to fileset, `env://NAME` to EnvironmentInput, `func://command` to RuntimeInput, `dep://a,b,c` to ExternalDependencyInput. Wired into `InProcessTaskHasher#expandInputs` so URIs hash identically to the object form. Negation supported on fileset URIs only (`!file://...`, `!glob://...`); env/func/dep negation throws. Unknown schemes throw `InvalidInputUriError` at config load — silent fallback would mask cache-correctness bugs from typos like `gob://**`. `{projectRoot}` / `{workspaceRoot}` tokens honored inside file/glob bodies. Schema documents the form; vis docs (`docs/configuration.mdx`) carry an Input Forms table. 18 unit tests in `__tests__/unit/parse-input-uri.test.ts` + 2 hasher integration tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 26  | Task-level runner tags ✅                  | moon#2099                                                   | Per-target `options.runnerTags: string[]` declares capability requirements; the CLI's `--runner-tags=gpu,slow` flag (or `VIS_RUNNER_TAGS` env) tells vis what the current runner supports. Filter is overlap-based: untagged tasks are the default lane (always eligible), tagged tasks only run when at least one tag overlaps with the runner set. CLI flag wins over env so a script can override a CI-injected default. Empty / whitespace-only tags collapse to "filter inactive" so `--runner-tags=` doesn't accidentally bind an empty filter. Filter applies before the graph is built, so skipped tasks never reach scheduling. 4 unit tests in `__tests__/task/target-options.test.ts` + 6 e2e tests in `__tests__/commands/run/run-runner-tags.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 27  | Docker scaffold lockfile pruning ✅        | turborepo prune --docker parity                             | `vis docker scaffold` rewrites the workspace lockfile so it carries only the focus closure, matching turbo's killer Docker-cache feature. Per-PM modules under `src/util/docker-lockfile/`: pnpm (v6 + v9 with separate `snapshots`), npm (v3+ path-keyed `packages` with nested `node_modules` resolution), yarn classic (seeded from per-project `package.json` deps — classic has no workspace block) + berry (uses `__metadata` block + `@workspace:` resolutions, handles implicit `npm:` protocol prefix), bun (JSONC `bun.lock` parser; `bun.lockb` skipped with migration hint). Dispatcher in `src/util/docker-lockfile/index.ts`; integration in `scaffoldDockerContext` (`src/util/docker.ts`) loops `LOCKFILE_FILES`, builds the closure `FocusProject[]` from each kept project's `package.json`, calls `pruneLockfile`, falls back to verbatim copy on `LockfilePruneError`. CLI: `vis docker scaffold --no-prune-lockfile` opts out. 13 unit tests in `__tests__/util/docker-lockfile.test.ts` covering all five PMs, plus per-pruner summary line emitted via `info`-level log. Required workspace override `'@yarnpkg/parsers>js-yaml': ^3.14.1` so berry parsing works (the published parser still calls v3 `safeLoad`).                                                                                                                                                                                                                                                                                                                                                                          |
| 28  | Signed remote cache artifacts ✅           | Section 4 Tier-2 (Turbo `TURBO_REMOTE_CACHE_SIGNATURE_KEY`) | HMAC-SHA256 signing for the HTTP backend lives in `task-runner/src/backends/http.ts`. `computeArtifactSignatureStream` streams `HMAC(secret, hash \|\| body)` off disk so multi-hundred-MB tarballs stay flat in memory; signature ships in `X-Artifact-Signature` on PUT and is verified on GET via `timingSafeEqual` (constant-time). Configured through `taskRunnerOptions.remoteCache.signing: { secret, verifyOnDownload }` in `vis-config.ts` — `secret` must be ≥16 chars (constructor throws on short keys); `verifyOnDownload` (default false) flips the client to reject downloads whose signature is missing or doesn't match. REAPI backend deliberately skips signing — gRPC integrity rides on sha256 content-addressing already (documented in `backends/types.ts`). No CLI flag — config-only by intent so a typo can't silently disable signing on a production cache.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 29  | Workspace-graph queries (`vis query`) ✅   | Section 4 Tier-2 (moon MQL, `nx graph --focus`)             | Covered by the existing `--query` selector grammar — a first-class flag on `list`, `run`, `ci`, `affected`, and `action-graph`. `vis list --query "tag=lang-ts && !test" --format=json` already emits the JSON project list; the shell-pipeline use case is served by `vis list --query ... --format=json \| jq`. A standalone `vis query` command would be thin alias sugar over this; not building it — the engine and JSON output already exist.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 30  | Glob targets (`app/...`) — ❌ not needed   | Section 4 Tier-2 (Bazel-style, moon supports)               | **Dropped — see Tier C.** Zero demand signal: a "competitor has it" Tier-2 item that never surfaced in issue mining (Sections 7–8, not even ★). `--projects=app/*` + tag selectors + the resolved #29 `--query` grammar give full functional coverage; `app/...` is pure path-syntax sugar, not a missing capability, and a parallel Bazel-style selector would split the grammar against #29's blessed path. Not on the roadmap.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 31  | Fingerprint plugins ✅                     | Section 4 Tier-2 (Turbo TS extensions)                      | `task:fingerprint(task, contributor)` hook in `VisHooks` fires from `InProcessTaskHasher#hashTask` after built-in inputs (filesets, runtime, env, framework-env) are gathered and before the hash is sealed. Bridged via `TaskRunnerOptions.onFingerprint` (vis wires it after the config spread so a config override can't disable the plugin pipeline) into a `FingerprintContributor` that exposes `contribute(key, value)`; contributions land in the runtime bucket under the `plugin:` namespace, then sort-and-merge in `computeTaskHash` so call order doesn't change the hash. Throws propagate intentionally — a buggy plugin fails the task before any cache lookup, locking out the silent-corruption failure mode. Public surface: `FingerprintContributor` + `FingerprintHook` re-exported from `@visulima/task-runner` and `@visulima/vis/config`. Tests cover dispatch, value-change, order-independence, and throw-propagates at both layers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 32  | Syscall-traced auto inputs (alt hash mode) | Vite Task fspy (Section 1.4)                                | Optional process-tracing input mode for `task-runner` so users can opt out of declaring `inputs`. Records read/openat syscalls, missing-file probes (negative lookups), and directory-listing reads — same surface vite-task delivers via fspy. Per-target opt-in (`hashMode: "trace"` in `vis-task.ts`) so the declared-inputs path stays the default for cache reproducibility. Implementation lanes: Linux (eBPF first, ptrace fallback), macOS (EndpointSecurity ES_EVENT_TYPE_NOTIFY_OPEN), Windows (ETW Microsoft-Windows-Kernel-File). Watch the SIGBUS-on-`/dev/shm` failure mode that fspy hits (vite-task #353/#354) — pick a transport that doesn't depend on shared-memory rings. Risk: trace mode breaks cache portability across runners with different syscall surfaces; document hard. Demand: ★★ (the universal "I forgot to add this to inputs" pain — moon, turbo, nx, lage all have years of issues on it).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 33  | Compound-command sub-task splitting        | Vite Task (Section 1.4)                                     | Auto-split `cmd1 && cmd2` (and `;` chains) on the boundary so each segment hashes + caches independently — partial cache hits when only one half's inputs change. Behind a per-target opt-in (`splitCompound: true`) so existing whole-script semantics stay default; replay ordering bugs in vite-task (#364) prove this isn't free. Pairs with #6 — finally tasks already model post-segment lifecycle. Cheap to prototype (token-split before spawn), expensive to harden (env carry-over, exit-code propagation, partial-restore correctness). Defer until a real noisy compound script surfaces in the wild; meanwhile, document the workaround (declare each step as a separate target with `dependsOn`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 34  | "All your tools, one config" docs pivot    | Vite+ `vite.config.ts` ergonomics (Section 1.4)             | Messaging-only — vis already supports `vis-config.ts` + per-package `vis.task.ts` overlay (item 9). The win is showing the equivalent of vite-task's single-file ergonomics: a worked README example where one `vis-config.ts` carries targets + cache + remote-cache + service registry + plugins + AI heal config, side-by-side with the comparable `vite.config.ts`. No code work; reframes the existing surface so the comparison reads "vis already does that, plus REAPI/MCP/heal/services/signed-cache" instead of "vis is more configuration." Pairs with item 35.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 35  | Lead-with-the-moat docs sweep ✅           | Section 1.4 differentiation read                            | Shipped. README `## Features` and `docs/index.mdx` "Key Features" both lead with Built-for-AI-agents (MCP / `ai heal` / worktree cache) → Production-grade caching (REAPI / `cache why` / signed) before any generic workspace-discovery framing. `docs/guides/why-vis.mdx` carries the full side-by-side matrix vs Vite Task / Turbo / Nx / moon plus When-to-pick / Migration / Honest-gaps sections and 6 deep links to concrete docs pages; README links into it. Residual (cosmetic, non-blocking): `docs/index.mdx` frontmatter `description:` still uses the old generic tagline.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 36  | Machine-output conformance audit           | Axis A (Design axes); competitive-analysis Themes M + Z     | Enforcement path for **Axis A**. One conformance test that walks every registered result-producing command and asserts: (a) exposes `--format` (or a documented back-compat `--json` alias), (b) stdout in JSON mode parses as a single valid document / NDJSON stream with nothing non-payload on stdout, (c) logs land on stderr only, (d) byte-stable across two runs with identical inputs, (e) exit code independent of `--format`. Also where the `--json`→`--format` convergence for the 7 legacy boolean-flag commands (`why`, `info`, `ignore`, `action-graph`, `toolchain`, `generate`, `cache why/hash`) is tracked. **Why defer:** the contract is already met case-by-case; this hardens it into a regression gate so future commands can't silently break the MCP/self-heal consumers. Effort S — mostly a registry walk + golden-output harness; no user-facing surface change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

## Tier C — explicitly out of scope

Not on the roadmap; either too costly or wrong product surface:

- Real-time distributed agents (Nx Agents) — Cobuilds-style is enough
- Polyglot first-class (Gradle/.NET/Python) — defer until clear demand
- Hermetic sandbox / network isolation — multi-quarter, narrow audience
- VS Code / JetBrains extensions — MCP server (item 5) covers this surface
- Web UI / cloud dashboard — out of scope without hosting commitment
- Persistent workers (Bazel) — narrow audience, no JS-land demand signal
- Component-level versioning (Bit) — different product
- Microfrontend dev proxy — niche unless target audience is there
- Versioning/changesets engine — `multi-semantic-release` covers it. _Caveat: `multi-semantic-release` does **not** cover provenance attestation — that's item 12's outbound scope, deliberately not pre-dismissed here._
- Knip-class unused-code detection — separate product surface
- Persistent-toolchain process (rushstack#1151) — meaningful only with persistent workers
- Glob targets (`app/...`, Bazel/moon-style subtree selector) — `--projects=app/*` + tags + the `--query` grammar fully cover it; zero demand signal in issue mining

---

## Suggested execution order

Quarters are illustrative; adjust to actual capacity.

**Q1: Foundation + first-impression (items 1, 3, 4, 7)**
Diagnostics + watch UX + cache retention + output styles. All small-to-medium, all directly competitive on a feature axis vs Turbo/Nx/moon. Ship together as "vis 1.0 polish."

**Q2: Strategic moats (items 2 ✅, 5 ✅, 5b ✅)**
REAPI gRPC ✅ + MCP with introspection ✅ + worktree-aware shared cache ✅. All three landed: REAPI unlocks bazel-remote/BuildBuddy as drop-in backends, MCP ships as a read-only agent surface (mutating commands stay human-invoked), and worktree-share unlocks parallel-agent UX. Standalone cache binary explicitly dropped — `bazel-remote` is the documented self-host.

**Q3: Devloop + correctness (items 6 ✅, 9 ✅, 10, 11 ✅, 13)**
Conditional/affected/finally tasks ✅ + config layering ✅ + cache restoration fidelity ✅ + shared services registry (cross-invocation lifecycle) + migration equivalence verification (item 13). Cache correctness locked down before `docker scaffold` scales; the "I keep restarting Postgres" papercut is the open item. Item 13 is the migration analogue — it gates the credibility of every adopt/leave claim and pairs with the outbound migrator (Tier B / 7.3 #9).

**Q4: AI moat (item 8 ✅)**
`vis ai heal` shipped end-to-end: read failure → propose → apply → validate by re-running → comment to PR/MR (GitHub Actions + GitLab CI), and `vis ai heal accept` lands the patch as a signed commit when an allow-listed maintainer comments `/vis heal accept`.

After the ranked items: pull from Tier B opportunistically, in order of community demand.

---

## Effort legend

- **S** — days, single contributor
- **M** — 1–4 weeks, single contributor
- **L** — 1–2 months, may need 2 contributors

## Assumptions worth challenging

- Items 1, 4, 7 assume the existing `run-summary.ts` and `task-runner` cache APIs can be extended cleanly. Verify before scoping.
- Item 2 assumes `@grpc/grpc-js` is acceptable as a runtime dep (~3 MB).
- ~~Item 5 assumes MCP SDK stability through 2026~~ — resolved: shipped against `@modelcontextprotocol/sdk` with a stable eight-tool contract; future tools are additive.
- ~~Item 8 assumes `vis ai` already has the primitives needed for CI invocation~~ — resolved: shipped on top of `aggregateFailureContext` / `runFixAnalysis` / `applyFixProposal` with no new primitives. Auto-commit-on-accept landed via `vis ai heal accept` with optional `@octokit/rest` + `@gitbeaker/rest` peer deps (lazy-loaded; the loader prompts to install on first need or fails loud in CI).
- Item 10 assumes the existing `server`/`utility` task types can attach to a registry without re-modeling task lifetimes.
- ~~Item 11 assumes the cache backend stores enough metadata to reproduce mtime/perms~~ — resolved: archive layer carries mtime + mode bits, no cache-format bump needed.
- Item 24 assumes (a) vis's existing PM detection (`src/pm/`) covers enough of autoskills' detection surface (config files + content patterns beyond `package.json` — `pyproject.toml`, `Gemfile`, `Cargo.toml`, gradle/.NET layouts) to drive matches without a parallel detector layer, and (b) `claude skills install <slug>` stays a stable command surface to embed in printed hints. If (a) is wrong, scope slips from S to M; if (b) drifts, the suggestions rot silently.
- Item 13 assumes (a) the inbound migrator parsers expose a reusable _intermediate graph model_ (not just a one-shot config emitter) so the source graph can be built without re-running turbo/nx/moon, and (b) vis's task-graph serialization is already deterministic enough (Axis A) to diff two graphs structurally without normalization drift. If (a) is wrong, the migrators need a parse/emit split first and effort slips M→L; (b) is load-bearing on item 11's locale-aware sort already shipping.
- Item 12 assumes (a) cosign/Sigstore is acceptable as an optional peer dep (CLI or `sigstore-js`), and (b) keyless OIDC signing only works where an OIDC token exists (CI: GitHub/GitLab/Buildkite). Local signing needs an explicit fallback — `cosign sign-blob --key` (KMS/file key) or "skip + warn outside CI" — decided before scoping, not after. Inbound (part a) has none of these constraints since it's already shipped.
- Tier C "explicitly out of scope" should be revisited once the top items land — community feedback may move things up.
