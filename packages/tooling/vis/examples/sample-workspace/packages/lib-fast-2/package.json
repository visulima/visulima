{
    "name": "lib-fast-2",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "echo 'Building lib-fast-2...' && sleep .2 && echo 'lib-fast-2 built!'",
        "lint": "echo 'Linting lib-fast-2... OK'",
        "test": "echo 'Testing lib-fast-2... 3 tests passed'",
        "dev": "echo 'lib-fast-2 dev server on port 3002' && while true; do sleep 1; echo '[lib-fast-2] heartbeat'; done"
    }
}
