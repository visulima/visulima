{
  "name": "lib-fast-8",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Building lib-fast-8...' && sleep .5 && echo 'lib-fast-8 built!' && mkdir -p dist && date > dist/.stamp",
    "dev": "echo 'lib-fast-8 dev server on port 3008' && while true; do sleep 1; echo '[lib-fast-8] heartbeat'; done",
    "lint": "echo 'Linting lib-fast-8... OK'",
    "test": "echo 'Testing lib-fast-8... 3 tests passed'"
  },
  "dependencies": {
    "lib-fast-3": "1.0.0",
    "lib-fast-5": "1.0.0"
  }
}
