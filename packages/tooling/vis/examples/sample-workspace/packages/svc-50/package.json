{
    "name": "svc-50",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "echo 'svc-50 built' && sleep 0.3 && mkdir -p dist && date > dist/.stamp",
        "dev": "echo 'svc-50 server starting on port 4050...' && echo 'Ready in 150ms' && while true; do sleep 2; echo \"[$(date +%H:%M:%S)] svc-50: request handled\"; done",
        "lint": "echo 'svc-50 lint OK'",
        "test": "echo 'svc-50 tests OK'"
    },
    "dependencies": {
        "lib-fast-10": "1.0.0",
        "lib-medium-15": "1.0.0"
    }
}
