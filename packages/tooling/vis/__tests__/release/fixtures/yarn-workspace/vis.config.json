{
    "release": {
        "defaultManaged": true,
        "acknowledgeUnstable": true,
        "channels": { "main": { "tag": "latest", "mode": "auto-publish" } },
        "publish": { "packManager": "yarn", "publishStrategy": "npm-publish-tarball" }
    }
}
