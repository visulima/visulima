/**
 * Public utility surface (`@visulima/tui/utils`).
 *
 * Formatting helpers shared by the time-bearing components (Timer, Stopwatch)
 * so their readouts stay consistent with one another.
 */
export { formatDuration } from "../utils/format-time";
