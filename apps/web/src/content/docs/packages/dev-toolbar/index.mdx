---
title: Visulima Dev Toolbar
description: A framework-agnostic development toolbar for Vite applications with built-in accessibility auditing, performance monitoring, SEO inspection, and an extensible app system.
---

import { Card, Cards } from "fumadocs-ui/components/card";

`@visulima/dev-toolbar` is a powerful, extensible development toolbar for any Vite-based project — React, Vue, Svelte, SolidJS, or plain HTML. Inspired by Astro DevToolbar, Vue DevTools, and Nuxt DevTools, it brings a consistent developer experience regardless of your framework.

The toolbar renders inside a Shadow DOM custom element for complete CSS isolation, communicates with the Vite dev server over type-safe RPC, and ships seven built-in apps covering the most common development workflows.

## Features


    
    
    
    
    
    


## Built-in Apps at a Glance

| App                                                            | ID                         | What it does                                        |
| -------------------------------------------------------------- | -------------------------- | --------------------------------------------------- |
| [Accessibility](/docs/dev-toolbar/built-in-apps/accessibility) | `dev-toolbar:a11y`         | axe-core WCAG audit with live element overlays      |
| [Performance](/docs/dev-toolbar/built-in-apps/performance)     | `dev-toolbar:performance`  | Web Vitals, resource timing, navigation metrics     |
| [SEO](/docs/dev-toolbar/built-in-apps/seo)                     | `dev-toolbar:seo`          | Social preview cards + meta tag audit               |
| [Timeline](/docs/dev-toolbar/built-in-apps/timeline)           | `dev-toolbar:timeline`     | Chronological event log from your app and libraries |
| [Module Graph](/docs/dev-toolbar/built-in-apps/module-graph)   | `dev-toolbar:module-graph` | Browse and filter Vite's live module graph          |
| [Vite Config](/docs/dev-toolbar/built-in-apps/vite-config)     | `dev-toolbar:vite-config`  | Inspect the resolved Vite configuration             |
| [Settings](/docs/dev-toolbar/built-in-apps/settings)           | `dev-toolbar:settings`     | Theme, toolbar behaviour, keyboard shortcuts        |

## How it Works

The toolbar is injected into the page by a Vite plugin as a `<dev-toolbar>` custom element. It renders a Preact UI inside Shadow DOM — so no styles leak in or out. Each "app" is a Preact component that receives RPC helpers and renders inside a resizable, draggable panel.

```
Browser                          Vite Dev Server
──────────────────────────────   ────────────────────────
<dev-toolbar> (Shadow DOM)  ←──  HTML injection (script tag)
  ├── Toolbar pill                ├── RPC handler
  ├── Panel canvas       ←──────→ ├── getViteConfig()
  │   ├── App: A11y               ├── getModuleGraph()
  │   ├── App: Performance        └── openInEditor()
  │   └── App: SEO
  └── Settings (localStorage)
```

## Keyboard Shortcuts

| Action                    | Default           |
| ------------------------- | ----------------- |
| Toggle toolbar open/close | `Alt`+`Shift`+`D` |
| Close active app / panel  | `Escape`          |

Both shortcuts are fully configurable in the [Settings app](/docs/dev-toolbar/built-in-apps/settings) or via the [plugin options](/docs/dev-toolbar/configuration).
