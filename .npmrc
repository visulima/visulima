# As of pnpm v11, .npmrc is read for authentication and registry
# configuration only. All other pnpm settings must live in
# pnpm-workspace.yaml (or ~/.config/pnpm/config.yaml for user-global
# overrides). See https://github.com/pnpm/pnpm/releases/tag/v11.0.0-rc.0
